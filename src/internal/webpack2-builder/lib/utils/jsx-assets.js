//const log = require('log4js').getLogger('wfh.jsx-assets');

exports.replaceAssetsAsync = replaceAssetsAsync;

function replaceAssetsAsync(node, replacements) {
	// let elType = node.arguments[0];
	// if (elType.type === 'Literal' && Array.isArray(node.arguments[1].properties)) {
	// 	for (let prop of node.arguments[1].properties) {
	// 		if (prop.value.type === 'Literal') {
	// 			if (prop.key.name === 'src') {
	// 				// log.warn(node.arguments[0]);
	// 			} else if (prop.key.name === 'href') {
	// 				// TODO
	// 			} else if (prop.key.name.toLowerCase() === 'srcset') {
	// 				// TODO
	// 			}
	// 		}
	// 	}
	// }
}
