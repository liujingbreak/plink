import fs from 'fs';

describe('RB tree', () => {
  it('smoke', () => {
    // eslint-disable-next-line no-console
    console.log('inside sample test', __filename);
    // eslint-disable-next-line no-console
    console.log('test import Node module', fs.readdirSync('.'));
  });
});

